# 练习79: 测试依赖覆盖
# 本练习文件只有注释，请在下方编写代码实现

"""
任务1: 依赖覆盖 - 基础示例
要求:
- 创建依赖函数 get_settings() 返回配置字典 {"db_url": "production-db", "debug": False}
- 创建路由 /config/ 使用该依赖，返回配置信息
- 使用 TestClient 测试该路由
- 在测试中覆盖依赖，返回测试配置 {"db_url": "test-db", "debug": True}
- 验证响应中返回的是测试配置
- 测试结束后清理覆盖
"""

"""
任务2: 依赖覆盖 - 外部服务模拟
要求:
- 实现依赖 get_external_api_data() 调用外部API（实际不调用，返回固定数据）
- 路由 /weather/ 使用该依赖返回天气数据
- 创建测试，覆盖依赖返回模拟数据
- 测试两种场景:
  1. 正常天气数据
  2. API错误（依赖返回None或抛出异常）
- 验证路由的正确响应
"""

"""
任务3: 依赖覆盖 - 用户认证
要求:
- 实现依赖 get_current_user() 验证token并返回用户
- 创建受保护路由 /profile/ 返回用户信息
- 测试中:
  1. 覆盖依赖返回普通用户 {"id": 1, "username": "testuser"}
  2. 覆盖依赖返回管理员用户 {"id": 2, "username": "admin", "is_admin": True}
  3. 覆盖依赖抛出401异常
- 验证每种场景的路由响应
"""

"""
任务4: 依赖覆盖 - 数据库会话
要求:
- 实现 get_db() 依赖返回数据库会话
- 创建CRUD路由:
  - POST /users/ - 创建用户
  - GET /users/{user_id} - 获取用户
- 测试中覆盖数据库会话为内存字典
- 测试创建和查询功能
- 验证数据正确性
"""

"""
任务5: 依赖覆盖 - pytest夹具
要求:
- 使用pytest编写测试
- 创建fixture app_client 和 override_db
- fixture自动处理依赖覆盖和清理
- 编写多个测试函数:
  - test_create_user()
  - test_get_user()
  - test_update_user()
  - test_delete_user()
- 每个测试使用独立的数据库覆盖
"""

"""
任务6: 依赖覆盖 - 多个依赖同时覆盖
要求:
- 创建两个依赖:
  1. get_cache() - 返回缓存客户端
  2. get_db() - 返回数据库客户端
- 创建路由 /data/{key}:
  - 先查缓存，缓存没有则查数据库
  - 将数据库结果写入缓存
- 测试中同时覆盖两个依赖
- 测试场景:
  1. 缓存命中
  2. 缓存未命中，从数据库读取
  3. 数据库也没有数据
"""

"""
任务7: 依赖覆盖 - 条件性覆盖
要求:
- 实现get_payment_service()依赖，生产环境调用真实支付服务
- 创建路由 /pay/ 处理支付
- 使用环境变量决定是否覆盖依赖
- 编写测试:
  - 测试环境自动覆盖为mock支付服务
  - 验证mock服务被正确调用
  - 检查支付参数正确性
"""

# 在下方编写你的代码实现
if __name__ == "__main__":
    # 运行测试：pytest test_file.py
    pass
